Permanent Fix:
# Add your user to the docker group
sudo usermod -aG docker $USER

# Log out and log back in (or run this to apply immediately in current shell)
newgrp docker

# Test without sudo
docker network ls

Network: csce413_assignment2_vulnerable_network
Web App IP: 172.20.0.10
Database IP: 172.20.0.11
br-b9f51b5c9130

# May need to include this in the command for MITM command: -i br-b9f51b5c9130

To run MITM script: sudo python3 capture_traffic.py
**Optional flags:**
- `-v` or `--verbose`: Show ALL traffic, not just interesting packets
- `-o FILE`: Specify output filename
- `-i INTERFACE`: Specify network interface (auto-detects by default)
- `-c COUNT`: Capture specific number of packets

#Port Knocking Demo to show it works -- You proteced SSH service
1. ssh user@172.20.0.40 -p 2222
2. cd port_knocking 
3. python3 knock_client.py --target 172.20.0.40 --sequence 1234,5678,9012
4. ssh user@172.20.0.40 -p 2222
#Port knocking Demo to show it protects after sequence intiated
5. python3 knock_client.py --target 172.20.0.40 --sequence 1234,9999,9012
#Run Port Knocking Demo
6. cd port_knocking
7. bash demo.sh 172.20.0.40
8. Check Port Knocking Logs: docker logs 2_network_port_knocking
dddd